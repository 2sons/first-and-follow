#include <stdio.h>

int main()
{

	return 0;
}

int getInput()
{
	int i;
	int numVariables;
	int numTerminals;
	printf("Variables: ");
	scanf(" %d", &numVariables);
	for (i = 0; i < numVariables; i++) {

	}
	printf("Terminals: ");
	scanf(" %d", &numTerminals);
	for (i = 0; i < numTerminals; i++) {

	}
	return 0;
}
